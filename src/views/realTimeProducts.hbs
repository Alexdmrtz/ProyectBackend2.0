<!-- realTimeProducts.hbs -->
<div>
    <h1 class="text-2xl font-bold my-3 dark:text-white">PRODUCTOS DESDE WS</h1>
    <section id="productList" class="grid grid-cols-3 place-content-between">
        {{#if products}}
            {{#each products}}
                <div id="product-{{this.id}}" class="max-w-sm bg-amber-100 border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
                    <a href="#">
                        <img class="rounded-t-lg" src="{{this.thumbnail}}" alt="" />
                    </a>
                    <div class="p-5">
                        <a href="#">
                            <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{this.title}}</h5>
                        </a>
                        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{this.description}}</p>
                        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400"><span class="font-bold">stock:</span> {{this.stock}}</p>
                        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400"><span class="font-bold">price: $</span>{{this.price}}</p>
                        <div class="flex justify-between">
                            <a href="#" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover-bg-blue-800 focus:ring-4 focus-outline-none focus-ring-blue-300 dark:bg-blue-600 dark:hover-bg-blue-700 dark:focus-ring-blue-800">
                                Read more
                                <svg class="w-3.5 h-3.5 ml-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
                                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
                                </svg>
                            </a>
                            <button onclick="deleteProduct({{this.id}})" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-black bg-red-700 rounded-lg hover-bg-red-800 focus-ring-4 focus-outline-none focus-ring-red-300 dark:bg-red-600 dark:hover-bg-red-700 dark:focus-ring-red-800">
                                Delete
                            </button>
                        </div>
                    </div>
                </div>
            {{/each}}
        {{else}}
            <div class="flex justify-center w-full">
                <h2 class="text-2xl font-bold my-3 dark:text-white">NO HAY PRODUCTOS</h2>
            </div>
        {{/if}}
    </section>
</div>

<!-- Incluir Socket.io -->
<script src="/socket.io/socket.io.js"></script>
<!-- Incluir tu archivo JavaScript para manejar las interacciones del cliente con WebSockets -->
<script src="realTimeProducts.js"></script>
